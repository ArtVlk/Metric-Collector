services:
  postgres:
    image: postgres:15-alpine
    container_name: metrics-db
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: metrics_db